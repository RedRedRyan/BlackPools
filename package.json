{
  "name": "black-pools",
  "version": "1.2.0",
  "description": "Confidential DeFi Lending Protocol on Fhenix CoFHE",
  "private": true,
  "scripts": {
    "install:all": "pnpm install -r",
    "compile": "pnpm -C contracts compile",
    "test": "pnpm -C contracts test",
    "deploy": "pnpm -C contracts hardhat run scripts/deploy.ts",
    "dev": "pnpm -C frontend dev"
  }
}
